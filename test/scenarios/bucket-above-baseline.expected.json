{
  "_rationale": "With 0% volatility, MC path equals deterministic baseline path. portfolio / baseline = 1.0; strict '> 1.0' check fails → refill suppressed. cashRefillFromSurplus = 0. cashEndingBalance = $5k. Verifies the trigger doesn't fire when the MC isn't actually outperforming the plan.",
  "probability": 100,
  "breakdownChecks": [
    { "index": 0, "field": "cashRefillFromSurplus", "value": 0, "tolerance": 0.01, "_note": "MC == baseline; strict > fails → no refill" },
    { "index": 0, "field": "cashEndingBalance", "value": 5000, "tolerance": 1 }
  ]
}
