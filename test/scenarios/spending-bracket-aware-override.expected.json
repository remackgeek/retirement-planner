{
  "_rationale": "Explicit taxable_first opts out of the smart default. Trad pull = conversion principal only ($50k). Spending and conv tax both flow through Taxable. Contrast: spending-bracket-aware-conv.json (same inputs, undefined → bracket_aware) shows withdrawalFromTraditional > $60k because spending pulls Trad up to bracket headroom.",
  "probability": 100,
  "tolerance": 1.0,
  "breakdownChecks": [
    { "index": 0, "field": "rothConversionGross", "value": 50000, "tolerance": 1, "_note": "Full conv year 1" },
    { "index": 0, "field": "withdrawalFromTraditional", "value": 50000, "tolerance": 100, "_note": "Conv only — taxable_first explicitly disables the bracket-aware Trad spending pull" },
    { "index": 0, "field": "withdrawalFromTaxable", "min": 60000, "max": 90000, "_note": "Spending + conv tax + LTCG cascade all from Taxable" }
  ]
}
